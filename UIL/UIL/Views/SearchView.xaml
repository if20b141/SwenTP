<UserControl x:Class="UIL.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UIL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Height="25" Margin="0,0,0,0" VerticalAlignment="Center">Search for:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Height="25" Margin="0,0,0,0" VerticalContentAlignment="Center" Text="{Binding SearchText}"/>
        <Button Grid.Row="0" Grid.Column="2" Height="25" MinWidth="50" Margin="0,0,0,0" Command="{Binding SearchCommand}">Search</Button>
        <Button Grid.Row="0" Grid.Column="3" Height="25" MinWidth="50" Margin="0,0,0,0" Command="{Binding ClearCommand}">Clear</Button>
    </Grid>
</UserControl>
