<UserControl x:Class="UIL.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UIL.Views"
             xmlns:functions="clr-namespace:DAL;assembly=DAL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>


        <TextBlock Visibility="Hidden" HorizontalAlignment="Center" VerticalAlignment="Center">Hüüüüüfe</TextBlock>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" Content="Test" Command="{Binding MapQuestCommand}"></Button>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="{Binding Text}"></TextBlock>
    </Grid>
</UserControl>