<Window x:Class="UIL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>


        <ContentControl Content="{Binding SelectedViewModel}"/>

        <StackPanel Orientation="Horizontal" Height="25" VerticalAlignment="Top">
            <Button MinWidth="160" Content="File" Command="{Binding UpdateViewCommand}" CommandParameter="File"/>
            <Button MinWidth="160" Content="Edit" Command="{Binding UpdateViewCommand}" CommandParameter="Edit"/>
            <Button MinWidth="160" Content="Help" Command="{Binding UpdateViewCommand}" CommandParameter="Help"/>
            <Button MinWidth="160" Content="View Tours" Command="{Binding UpdateViewCommand}" CommandParameter="ViewTours"/>
            <Button MinWidth="160" Content="Tours" Command="{Binding UpdateViewCommand}" CommandParameter="Tours"/>
        </StackPanel>


    </Grid>
</Window>